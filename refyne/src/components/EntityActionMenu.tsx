import { useEffect, useRef, useState } from 'react'; import { MoreVertical } from 'lucide-react';
export type MenuAction = { label: string; onClick: () => void; danger?: boolean; icon?: React.ReactNode };
export function EntityActionMenu({ actions }: { actions: MenuAction[] }) { const [open, setOpen] = useState(false); const ref = useRef<HTMLDivElement>(null); useEffect(() => { const close = (event: MouseEvent) => { if (!ref.current?.contains(event.target as Node)) setOpen(false); }; const esc = (event: KeyboardEvent) => event.key === 'Escape' && setOpen(false); document.addEventListener('mousedown', close); document.addEventListener('keydown', esc); return () => { document.removeEventListener('mousedown', close); document.removeEventListener('keydown', esc); }; }, []); return <div ref={ref} className="relative" onClick={e => e.stopPropagation()}><button onClick={() => setOpen(!open)} className="rounded-lg p-2 hover:bg-slate-100" aria-label="Actions"><MoreVertical size={18}/></button>{open && <div className="absolute right-0 z-30 mt-1 w-44 rounded-xl border bg-white p-1 shadow-lg">{actions.map(action => <button key={action.label} onClick={() => { setOpen(false); action.onClick(); }} className={`flex w-full items-center gap-2 rounded-lg px-3 py-2 text-left text-sm hover:bg-slate-50 ${action.danger ? 'text-rose-600 hover:bg-rose-50' : ''}`}>{action.icon}{action.label}</button>)}</div>}</div>; }
