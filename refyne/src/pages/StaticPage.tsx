import { useAuth } from '../contexts/AuthContext'; export function StaticPage({ title, description, profile }: { title: string; description?: string; profile?: boolean }) { const { user } = useAuth(); return <div className="max-w-3xl p-5 sm:p-8"><p className="text-sm font-semibold text-violet-600">REFYNE</p><h1 className="mt-1 text-3xl font-bold">{title}</h1>{profile ? <section className="card mt-6 p-6"><dl className="grid gap-5 sm:grid-cols-2"><div><dt className="text-xs uppercase text-slate-400">Full name</dt><dd className="mt-1">{user?.full_name || 'No data available'}</dd></div><div><dt className="text-xs uppercase text-slate-400">Email</dt><dd className="mt-1">{user?.email || 'No data available'}</dd></div><div><dt className="text-xs uppercase text-slate-400">Account ID</dt><dd className="mt-1 break-all">{user?.id || 'No data available'}</dd></div></dl></section> : <div className="card mt-6 p-6 text-slate-600">{description || 'No data available.'}</div>}</div>; }
