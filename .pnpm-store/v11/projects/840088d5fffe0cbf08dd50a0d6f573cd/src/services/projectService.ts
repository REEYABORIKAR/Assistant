import type { Project, Workspace } from '../types/models'; import { api } from './api';
export const projectService = { list: () => api<Project[]>('/api/projects'), get: (id: string) => api<Project>(`/api/projects/${id}`), create: (name: string, description: string) => api<Project>('/api/projects', { method: 'POST', body: JSON.stringify({ name, description: description || null }) }), update: (id: string, values: Pick<Project, 'name' | 'description'>) => api<Project>(`/api/projects/${id}`, { method: 'PUT', body: JSON.stringify(values) }), remove: (id: string) => api<void>(`/api/projects/${id}`, { method: 'DELETE' }), duplicate: (id: string) => api<Project>(`/api/projects/${id}/duplicate`, { method: 'POST' }), workspace: (id: string) => api<Workspace>(`/api/projects/${id}/workspace`) };
