import { Navigate, Outlet, Route, Routes, useLocation } from 'react-router-dom'; import { AuthProvider, useAuth } from './contexts/AuthContext'; import { AppLayout } from './components/layout/AppLayout'; import { ProjectsPage } from './pages/ProjectsPage'; import { WorkspacePage } from './pages/WorkspacePage'; import { DashboardPage } from './pages/DashboardPage'; import { SettingsPage } from './pages/SettingsPage'; import { LoginPage } from './pages/LoginPage'; import { DocumentsPage } from './pages/DocumentsPage'; import { ConversationsPage } from './pages/ConversationsPage'; import { StaticPage } from './pages/StaticPage';
function Protected() { const { user, loading } = useAuth(), location = useLocation(); if (loading) return <div className="grid min-h-screen place-items-center">Loading Refyne…</div>; return user ? <AppLayout><Outlet/></AppLayout> : <Navigate to="/login" state={{ from: location.pathname }} replace/>; }
export default function App() { return <AuthProvider><Routes><Route path="/login" element={<LoginPage/>}/><Route path="/app" element={<Protected/>}><Route index element={<Navigate to="projects" replace/>}/><Route path="projects" element={<ProjectsPage/>}/><Route path="projects/:projectId" element={<WorkspacePage/>}/><Route path="projects/:projectId/chat/:conversationId" element={<WorkspacePage/>}/><Route path="conversations" element={<ConversationsPage/>}/><Route path="documents" element={<DocumentsPage/>}/><Route path="dashboard" element={<DashboardPage/>}/><Route path="settings" element={<SettingsPage/>}/><Route path="templates" element={<StaticPage title="Templates" description="No template API is currently available. Templates will appear here when supported by the backend."/>}/><Route path="profile" element={<StaticPage title="Profile" profile/>}/><Route path="help" element={<StaticPage title="Help & Support" description="For account or service support, contact your Refyne administrator."/>}/></Route><Route path="*" element={<Navigate to="/app/projects" replace/>}/></Routes></AuthProvider>; }
