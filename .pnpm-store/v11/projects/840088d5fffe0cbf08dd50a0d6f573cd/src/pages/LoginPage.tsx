import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import { useAuth } from '../contexts/AuthContext';
export function LoginPage() { const { login } = useAuth(); const navigate = useNavigate(); const [email, setEmail] = useState(''), [password, setPassword] = useState(''), [error, setError] = useState(''), [busy, setBusy] = useState(false); const submit = async (event: React.FormEvent) => { event.preventDefault(); setBusy(true); setError(''); try {       await login(email, password);
      navigate('/app/projects?create=true', { replace: true }); } catch (e) { setError(e instanceof Error ? e.message : 'Unable to sign in.'); } finally { setBusy(false); } }; return <main className="grid min-h-screen place-items-center bg-[#10152c] p-4"><form className="w-full max-w-sm rounded-2xl bg-white p-7 shadow-xl" onSubmit={submit}><h1 className="text-2xl font-bold">Sign in to Refyne</h1><p className="mt-2 text-sm text-slate-500">Use your Refyne account to access your projects.</p><label className="label mt-6 block">Email<input required type="email" className="field" value={email} onChange={e => setEmail(e.target.value)}/></label><label className="label mt-4 block">Password<input required type="password" className="field" value={password} onChange={e => setPassword(e.target.value)}/></label>{error && <p className="mt-4 text-sm text-rose-600">{error}</p>}<button className="btn-primary mt-6 w-full" disabled={busy}>{busy ? 'Signing in…' : 'Sign in'}</button></form></main>; }
