export function SettingsPage() { return <div className="max-w-4xl p-5 sm:p-8"><p className="text-sm font-semibold text-violet-600">WORKSPACE</p><h1 className="mt-1 text-3xl font-bold">Settings</h1><div className="card mt-6 p-6"><h2 className="font-semibold">Available settings</h2><p className="mt-2 text-sm text-slate-500">The current backend has no persisted settings API. Controls are intentionally not shown until settings can be saved securely.</p></div><div className="card mt-4 p-6"><h2 className="font-semibold">AI configuration</h2><p className="mt-2 text-sm text-slate-500">The application uses the configured backend URL and authenticated JWT. AI-generation configuration is not exposed by the backend.</p></div></div>; }
